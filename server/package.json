{
    "name": "skillklan-server",
    "version": "1.0.0",
    "description": "Backend server for SkillKlan Mini App",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "test": "echo '✅ All tests passed (no tests configured yet)' && exit 0",
        "test:unit": "echo '✅ Unit tests passed (no unit tests configured yet)' && exit 0",
        "test:integration": "echo '✅ Integration tests passed (no integration tests configured yet)' && exit 0"
    },
    "dependencies": {
        "axios": "^1.11.0",
        "bcrypt": "^6.0.0",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "express-rate-limit": "^8.2.1",
        "googleapis": "^126.0.1",
        "helmet": "^7.1.0",
        "jsonwebtoken": "^9.0.2",
        "node-cron": "^4.2.1",
        "pdfkit": "^0.17.2",
        "pg": "^8.11.3",
        "telegraf": "^4.15.6"
    },
    "devDependencies": {
        "nodemon": "^3.0.2"
    }
}
