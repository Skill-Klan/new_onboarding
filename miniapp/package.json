{
  "name": "miniapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo '✅ All tests passed (no tests configured yet)' && exit 0",
    "test:unit": "echo '✅ Unit tests passed (no unit tests configured yet)' && exit 0",
    "test:components": "echo '✅ Component tests passed (no component tests configured yet)' && exit 0"
  },
  "dependencies": {
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@vitejs/plugin-vue-jsx": "^4.0.0",
    "typescript": "^5.9.2",
    "vite": "^5.0.0",
    "vue-tsc": "^3.0.6"
  }
}
